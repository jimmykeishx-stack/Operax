"use client";

import { useState } from "react";
import { Bar, BarChart, ResponsiveContainer, Tooltip } from "recharts";
import { Bell, CalendarDays, CheckCircle2, X } from "lucide-react";
import { Avatar, Breadcrumbs, Button, Card, DataTable, Dialog, EmptyState, Input, Modal, Pagination, SearchBox, Select, Skeleton, StatisticCard, StatusBadge, Tabs } from "@/components/ui";

const chartData = [{ name: "Mon", jobs: 18 }, { name: "Tue", jobs: 27 }, { name: "Wed", jobs: 21 }, { name: "Thu", jobs: 35 }, { name: "Fri", jobs: 24 }];
function Section({ title, children }: React.PropsWithChildren<{ title: string }>) { return <section className="space-y-4"><h2 className="text-lg font-semibold">{title}</h2><Card>{children}</Card></section>; }

export default function ComponentShowcase() {
  const [activeTab, setActiveTab] = useState("Overview");
  const [modalOpen, setModalOpen] = useState(false);
  const [dialogOpen, setDialogOpen] = useState(false);
  const [toast, setToast] = useState(false);
  return <div className="space-y-10 py-4 sm:py-8"><div><Breadcrumbs items={["Operax", "Foundation", "Design System"]} /><div className="mt-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">PRD foundation</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Component showcase</h1><p className="mt-2 max-w-2xl text-muted-foreground">Reusable, accessible interface patterns for the Operax operations workspace.</p></div><Button onClick={() => setToast(true)}><Bell className="h-4 w-4" />Show toast</Button></div></div><div className="grid gap-6 xl:grid-cols-2"><Section title="Buttons"><div className="flex flex-wrap gap-3"><Button>Primary action</Button><Button variant="secondary">Secondary</Button><Button variant="ghost">Ghost</Button><Button variant="danger">Destructive</Button><Button disabled>Disabled</Button></div></Section><Section title="Inputs, selects and search"><div className="grid gap-4 sm:grid-cols-2"><Input label="Job title" placeholder="Enter a job title" /><Select label="Priority" defaultValue="normal"><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></Select><div className="sm:col-span-2"><SearchBox placeholder="Search jobs, locations, or technicians" /></div></div></Section><Section title="Status badges and avatars"><div className="flex flex-wrap items-center gap-3"><StatusBadge>New</StatusBadge><StatusBadge status="info">Assigned</StatusBadge><StatusBadge status="warning">Blocked</StatusBadge><StatusBadge status="success">Completed</StatusBadge><StatusBadge status="danger">Overdue</StatusBadge><Avatar name="Sarah Mwangi" size="sm" /><Avatar name="Peter Otieno" /><Avatar name="Alice Wanjiku" size="lg" /></div></Section><Section title="Statistic cards"><div className="grid gap-3 sm:grid-cols-3"><StatisticCard label="Open jobs" value="24" change="+8% this week" /><StatisticCard label="In progress" value="9" change="On target" /><StatisticCard label="Blocked" value="3" change="Needs attention" /></div></Section><Section title="Tabs and pagination"><Tabs active={activeTab} onChange={setActiveTab} tabs={["Overview", "Activity", "Files"]} /><p className="mt-4 text-sm text-muted-foreground">Selected tab: <span className="font-semibold text-foreground">{activeTab}</span></p><div className="mt-5"><Pagination /></div></Section><Section title="Date picker"><label className="grid max-w-xs gap-1.5 text-sm font-medium">Scheduled date<div className="relative"><CalendarDays className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><input type="date" className="h-10 w-full rounded-lg border bg-card pl-10 pr-3 text-sm text-card-foreground" /></div></label></Section><Section title="Table"><DataTable /></Section><Section title="Chart"><div className="h-52" aria-label="Jobs completed by weekday"><ResponsiveContainer width="100%" height="100%"><BarChart data={chartData}><Tooltip cursor={{ fill: "var(--muted)" }} /><Bar dataKey="jobs" fill="var(--primary)" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></div></Section><Section title="Empty and loading states"><EmptyState title="No jobs found" description="New work will appear here once a supervisor creates it." /><div className="mt-5 space-y-3"><Skeleton className="h-4 w-1/3" /><Skeleton className="h-10 w-full" /><Skeleton className="h-10 w-4/5" /></div></Section><Section title="Modals and dialogs"><div className="flex flex-wrap gap-3"><Button onClick={() => setModalOpen(true)}>Open modal</Button><Button variant="secondary" onClick={() => setDialogOpen(true)}>Open dialog</Button></div></Section></div><Modal open={modalOpen} onClose={() => setModalOpen(false)} title="Create job"><p className="text-sm text-muted-foreground">This modal contains a focused task and supports closing with Escape, the close button, or the backdrop.</p><div className="mt-5 flex justify-end gap-2"><Button variant="ghost" onClick={() => setModalOpen(false)}>Cancel</Button><Button onClick={() => setModalOpen(false)}>Continue</Button></div></Modal><Dialog open={dialogOpen} onClose={() => setDialogOpen(false)} title="Archive confirmation"><p className="text-sm text-muted-foreground">Archive closed jobs from June? This action can be restored by an administrator.</p><div className="mt-5 flex justify-end gap-2"><Button variant="ghost" onClick={() => setDialogOpen(false)}>Cancel</Button><Button variant="danger" onClick={() => setDialogOpen(false)}>Archive jobs</Button></div></Dialog>{toast && <div className="fixed bottom-5 right-5 z-[70] flex max-w-sm items-start gap-3 rounded-xl border bg-card p-4 shadow-xl" role="status"><CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-success" /><div><p className="font-semibold">Changes saved</p><p className="mt-0.5 text-sm text-muted-foreground">The component system is ready to use.</p></div><button className="ml-2 rounded p-1 text-muted-foreground hover:bg-muted" aria-label="Dismiss notification" onClick={() => setToast(false)}><X className="h-4 w-4" /></button></div>}</div>;
}
